{
  "name": "node-canvas-renderer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "start": "node src/index.js",
    "lint": "eslint --fix \"./src/**/*.js\"",
    "staged": "lint-staged --relative"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.8.0",
    "discord.js": "^12.5.3",
    "dotenv": "^10.0.0",
    "gifencoder": "^2.0.1",
    "matter-js": "^0.17.1"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "eslint": "^7.30.0",
    "husky": "^7.0.1",
    "lint-staged": "^11.0.0"
  },
  "lint-staged": {
    "src/**/*.js": [
      "eslint"
    ]
  }
}